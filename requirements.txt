# Core dependencies for RVC Inference Service
# Production-grade voice conversion engine

# PyTorch ecosystem
torch>=2.0.0
torchaudio>=2.0.0

# HuBERT feature extraction - choose ONE:
# Option 1: transformers (RECOMMENDED - easier to install on Windows)
transformers>=4.30.0
# Option 2: fairseq (official, but harder to install on Windows)
# pip install fairseq
# fairseq>=0.12.0

# Audio processing
librosa>=0.10.0
soundfile>=0.12.0
numpy>=1.24.0
scipy>=1.10.0

# Pitch extraction (optional - for RMVPE)
# praat-parselmouth>=0.4.3

# Vector similarity search
# Use faiss-gpu for GPU support, faiss-cpu for CPU-only
faiss-cpu>=1.7.4
# faiss-gpu>=1.7.4  # Uncomment for GPU support

# API server
fastapi>=0.100.0
uvicorn[standard]>=0.22.0
python-multipart>=0.0.6

# Testing
pytest>=7.0.0
pytest-asyncio>=0.21.0
httpx>=0.24.0
